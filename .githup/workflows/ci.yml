Name the workflow

Give the workflow a title (e.g., “CI – Test, Coverage, Docker Build & Run”).

Define triggers

Specify when the workflow runs (e.g., on every push or pull request).

Set up a test job

Run on a GitHub-hosted machine (Ubuntu).

Checkout the repository code.

Set up Python.

Install dependencies from requirements.txt.

Run tests with coverage.

Save/upload the coverage report.

Set up a Docker job (runs only if tests pass)

Checkout the repository code.

Build the Docker image using the Dockerfile.

Run the container in the background.

Wait until the container is ready to accept requests.

Verify the container by making an HTTP request to the app.

Show logs for debugging.

Stop and remove the container after verification.